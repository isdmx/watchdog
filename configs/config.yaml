watchdog:
  namespaces:
    - "default"
    - "production"
  labelSelectors:
    app: "temporary"
    cleanup: "true"
  scheduleInterval: "1m"
  maxPodLifetime: "1h"
  dryRun: true  # Set to true for testing

logging:
  mode: "development"
  level: "debug"
